{
    "compilerOptions": {
        "moduleResolution": "node",
        "strict": true,
        "strictNullChecks": false, // This is required to make DDL's dot-path's work. I'm not sure why. TODO Investigate if we can turn it off. 
        "lib": [
            "ESNext",
            "DOM"
        ]
    },
    "include": [
        "./src"
    ]
}